# CrossPoint Reader 项目愿景与范围

[English](./SCOPE.md) | [简体中文](./SCOPE-ZH.md) | [日本語](./SCOPE-JA.md)

CrossPoint Reader 的目标是为基于 ESP32 的电子阅读器打造高效、开源的阅读体验。项目始于 Xteink 硬件（X3、X4），它们仍是主要目标设备，但 CrossPoint 正明确扩展支持范围，面向更广泛的小型 ESP32 墨水屏阅读器生态。我们认为，专用电子阅读器应当把一件事做到极致：**帮助用户专注阅读。**

## 1. 核心使命

提供轻量、高性能的固件，充分发挥基于 ESP32 的电子阅读器硬件潜力；相比“大而全”的功能，更重视可读性、性能和易用性。

CrossPoint **不是**包罗万象的固件，也**不只**面向 Xteink。我们希望建立一套整洁、可维护、可供社区继续开发的代码，并使其能够运行在多种 ESP32 电子阅读器设备上（ESP32-C3、ESP32-S3 及相近型号）。每项被接受的改动都应让这一目标更容易实现，而不是更困难。设备专用代码应位于 HAL / SDK 边界之后，使阅读器核心保持可移植性。

## 2. 指导原则：弥补原厂固件的不足

CrossPoint 的存在，是为了做好原厂固件做得不够好或完全没有实现的事情。新工作会根据以下界限进行评估：

* **原厂固件是否已经把这件事做好了？** 我们应达到或超过它的水平
* **另一个流行的 CrossPoint 分支是否已经很好地解决了这个问题？** 如果是，而且它不属于核心阅读体验，我们通常会将其留给该分支，例如统计功能
* **这是否能直接改善阅读体验或固件的长期可维护性？** 如果不能，则不在项目范围内

## 3. 当前重点

随着项目开始支持更多 ESP32 电子阅读器设备，我们正有意收窄范围，以便整合代码库。

此阶段的优先事项如下：

* **内存占用：** 减少 DRAM 使用量和堆碎片。ESP32-C3 是资源最紧张的目标设备，决定了资源上限，但这些优化会使我们支持的所有 ESP32 型号受益。
* **闪存占用：** 缩减二进制体积，为更多设备目标和功能留出空间。
* **代码清理：** 重构代码、删除无用代码、收紧抽象并提高可读性。
* **阅读体验：** EPUB 解析与渲染、排版、断词、行距、字体处理以及可读性改进。

### 暂停接收的领域

在本通知解除前，涉及以下领域的 PR 将被关闭。现在添加这些内容会显著增加代码清理和多设备支持工作的难度：

* **新主题。** 现有主题功能暂不扩展。
* **新的外部网络连接器。** 包括同步引擎、云存储客户端、现有功能之外的 OPDS 扩展、远程文件访问，以及任何新增的“与服务器通信”功能。我们现在已有自己的 CrossPoint KOSync 服务器，可在 API 层与 Hardcover 等第三方系统同步，无需让固件变得臃肿。如果你有兴趣参与，这个同步服务器同样是开源的。

如果不确定你的想法是否属于这些类别，请先发起 Discussion。

## 4. 项目范围

### 范围内

*以下功能会直接改善设备的核心用途。*

* **用户体验：** 例如阅读器内部和固件导航中的易用界面与交互，包括按键映射、书籍加载，以及书签等书籍导航功能。
* **文档渲染：** 例如支持文档渲染（主要是 EPUB）及改进渲染引擎。
* **格式优化：** 例如在设备能力范围内高效解析 EPUB（CSS/图像）和其他文档。
* **排版与可读性：** 例如自定义字体支持、断词引擎和可调行距。
* **电子墨水屏驱动改进：** 例如减少全屏闪烁（残影管理）并改善整体渲染。
* **书库管理：** 例如以简单、直观的方式整理和浏览藏书。
* **本地传输：** 例如通过基础 Web 服务器，或公开且广泛采用的标准，以简单的“拉取”方式加载书籍。
* **语言支持：** 例如在阅读器和各类界面中支持多种语言。
* **参考工具：** 例如本地词典查询，通过快速提供离线释义来帮助理解，同时不打断专注状态。
* **时钟显示（取决于设备）：**

| 设备 | 范围 |
| -- | -- |
| X3 | X3 使用独立的 DS3231 RTC，可在睡眠周期之间保持准确时间，因此可以视为可靠的实时时钟。 |
| X4 | X4 依赖 ESP32-C3 的内部 RTC，深度睡眠期间会产生明显漂移。NTP 同步可以校正时间，但需要围绕唤醒时或按需连接互联网设计适当的用户体验。这与下方的**主动联网**部分存在一定冲突；如果你认为此功能有用，请先发起 Discussion 讨论相关 UX。 |

### 范围外

*以下功能会损害设备的稳定性、可维护性或核心使命，因此不予接受。*

* **交互式应用：** 不加入记事本、计算器或游戏。这些功能更适合其他分支，不属于 CrossPoint 的重点。
* **写作 / 创作工具：** 不加入键入式笔记、日记或编辑器。设备的输入硬件和 RAM 并不适合此类用途，且已有其他分支在探索这一方向。
* **主动联网：** 不加入 RSS 阅读器、新闻聚合器或 Web 浏览器。后台 Wi-Fi 会消耗电池，并增加单核 CPU 的负担。
* **PDF 渲染：** PDF 是固定版式文档，渲染时需要将页面作为图像显示，而不是可重排文本，因此必须频繁平移和缩放，在墨水屏上体验不佳。以当前硬件级别而言，此功能不在范围内。

## 5. 行动倡议

以下领域目前最需要贡献者协助。如果你想承担其中一项，请先发起 Discussion 或 issue，以便我们协调工作。

### 主题系统：将主题移出固件

我们希望将主题完全从固件中抽离，使其不再占用闪存，改为从 SD 卡加载。这会直接支持当前缩减闪存占用和清理代码的重点。

* **状态：** [@itsthisjustin](https://github.com/itsthisjustin) 计划最终处理此项工作，但也非常欢迎其他人更早认领。
* **重要性：** 每个内置主题都会占用闪存，而我们更希望将这些空间用于渲染、字体或未来的设备支持。从 SD 卡加载主题也允许用户无需重新构建固件即可自定义，并可进一步实现从 SD 卡加载字体，从而改善 UI 的语言支持。
* **如何认领：** 开始前请在相关 Discussion 中留言，或新建一个 Discussion。

### 发现原厂固件的其他不足

我们希望有人协助整理原厂固件（以及其他流行 CrossPoint 分支）处理不佳或完全未处理的问题，为后续工作建立明确的目标清单。我们尤其关注：

* **RTL（从右到左）文本支持：** 阿拉伯语、希伯来语、波斯语及类似文字。
* **原厂固件和各分支支持不足的语言：** 尤其是需要字形塑造、复杂布局或非拉丁字体支持，而目前无人妥善处理的语言。
* **其他不足：** 渲染边缘情况、无障碍问题、输入异常，以及原厂固件表现不佳且现有分支尚未修复的任何问题。

如果你能使用其中一种语言阅读或操作设备，你的反馈即使不包含代码也非常有价值。请发起 Discussion 并提供具体示例（截图、EPUB 样本、预期行为与实际行为对比），我们会据此确定优先级。

## 6. 资金与贡献者可持续性

CrossPoint 使用 [Royalty.dev](https://royalty.dev)（没错，这是 [@itsthisjustin](https://github.com/itsthisjustin) 开发的产品）为贡献者提供资金支持。社区对此存在一些争议，因此我们在这里直接说明其目的。

**为什么这样做：**

* 回应社区希望回馈贡献者的大量诉求，维持贡献者和维护者的长期参与。
* 鼓励贡献者投入*核心*项目，而不是另建相互竞争的分支。
* 帮助购买新的 ESP32 设备，以便将 CrossPoint 移植到更多硬件。
* 为项目提供一条可信的长期可持续发展路径。

**运作方式：**

* 资金根据贡献者对代码库的影响和参与项目的时间自动分配。
* 开放资助后的最初几小时内筹得了超过 **600 美元**，说明确实存在这方面的需求。
* 具体的评分方法发布于 <https://app.royalty.dev/transparency>。

**这些规则并非一成不变。** 随着我们了解哪些方式适合本项目，可以调整权重、资格和分配规则。如果你对资金分配有疑虑或建议，请发起 Discussion。我们的目标是建立一个公平认可实际贡献者的制度，而不是一开始就追求完美。
